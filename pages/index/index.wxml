<!--index.wxml-->
<view class="container">
    <Image inner-data="{{componentData}}"></Image>
    <title title-data="{{titleData}}"></title>
    <card card="{{arr}}" bindcard="card"></card>
    <title title-data="{{titleData2}}"></title>
    <card card="{{arr}}" bindcard="card"></card>
</view>
